#include <stdio.h>

int main7() {

	// 연산자 
	// 1. 대입연산자
	// =
	// 오른쪽에 있는 데이터 값을 왼쪽에 있는 공간(변수)에 대입한다.

	int num1;
	num1 = 3;
	 
    int num2 = 4;

	num1 = num2; //오른쪽은 값. 왼쪽은 공간으로 사용된다. 값num2를 공간num1에 대입한다.
	num1 = num1; //왼쪽(공간)의 num1과 오른쪽(값)의 num1은 다르다.






	return 0;

}